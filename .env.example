# Optional local development secrets loaded by .envrc.
# Copy this file to .env; never commit real credentials.
# Task API keys are imported by ene-core at boot and remain in the vault.
ENE_AI__TASKS__CHAT__API_KEY=
