# `ene-common` — 移動済み

> **Status:** 吸収済み（API v2）。本ページはスタブです。

旧 `ene-common` クレートはワークスペースメンバーとしては存在しません。

| 旧エクスポート | 現在 |
|---|---|
| `Truncate` / `TruncateResult` | [`ene_config::truncate`](./ene-config.md)（`ene_config::{Truncate, TruncateResult}`） |
| ツール向け再エクスポート | [`ene-tool-common`](./ene-tool-common.md)（`ene_tool_common::truncate::{Truncate, TruncateResult}`） |

[API v2](../architecture/api-v2.md) を参照してください。
