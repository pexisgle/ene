# `ene-common` — Moved

> **Status:** Absorbed (API v2). This page is a stub.

The former `ene-common` crate no longer exists as a workspace member.

| Former export | Now |
|---|---|
| `Truncate` / `TruncateResult` | [`ene_config::truncate`](./ene-config.md) (`ene_config::{Truncate, TruncateResult}`) |
| Tool-facing re-export | [`ene-tool-common`](./ene-tool-common.md) (`ene_tool_common::truncate::{Truncate, TruncateResult}`) |

See [API v2](../architecture/api-v2.md).
