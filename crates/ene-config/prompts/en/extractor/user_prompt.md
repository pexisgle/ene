Conversation turn:
{conversation}

Pattern hints (optional assist from regex detectors — keep, revise, or discard; also add important facts not listed):
{pattern_hints}

Extract memory candidates from this turn. Choose kinds and decide what is worth persisting. Output JSON only.
