// Placeholder — will be populated in #81 (Context Budget Manager), #79 (Rolling Compression), #80 (Session Split Replacement)

/// Context Budget Manager and rolling context compression.
pub struct ContextManager;
