//! Pre-turn input analysis and turn intent classification.
//!
//! This module is a placeholder for future pre-turn analysis logic (#82).
//! Currently, all pre-turn logic lives in [`crate::CognitionEngine::before_turn`].

/// Pre-turn analysis: input classification, turn intent, memory retrieval needs.
///
/// Unit struct with no state; all logic is invoked through [`crate::CognitionEngine`].
pub struct PreTurnAnalyzer;
