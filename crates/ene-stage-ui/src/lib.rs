//! Generated Slint components for the Stage client.
//!
//! Isolated so `slint-build` output (which uses `unwrap`) cannot fail the
//! workspace clippy contract.

#![allow(dead_code, unused, clippy::all, clippy::pedantic, clippy::nursery)]

slint::include_modules!();
