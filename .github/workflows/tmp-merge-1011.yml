name: tmp-merge-1011

on:
  push:
    branches: [main]
    paths: ['.github/workflows/tmp-merge-1011.yml']

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: codex/ask-user-protocol
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.98.0
        with:
          components: rustfmt
      - name: Merge current parent and resolve known semantic conflicts
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin codex/approval-before-dispatch
          git merge --no-edit origin/codex/approval-before-dispatch || true
          python3 - <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"expected conflict block not found in {path}")
              p.write_text(text.replace(old, new, 1))

          replace(
              'apps/ene-core/src/http/routes.rs',
              '''<<<<<<< HEAD
    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, Health, HistoryResponse, JobView,
=======
    AffectView, AnswerJobRequest, ApprovalView, ArtifactView, BackupResponse, CharacterView,
    ClaimResourceRequest, CompactResponse, CreateScheduleRequest, CreateSessionRequest,
    EndSessionRequest, ExclusiveSnapshot, GreetingView, Health, HistoryResponse, JobView,
>>>>>>> origin/codex/approval-before-dispatch
''',
              '''    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, GreetingView, Health,
    HistoryResponse, JobView,
''')

          replace(
              'crates/ene-api/src/client.rs',
              '''<<<<<<< HEAD
    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, Health, HistoryResponse,
=======
    AffectView, AnswerJobRequest, ApprovalView, ArtifactView, BackupResponse, CharacterView,
    ClaimResourceRequest, CompactResponse, CreateScheduleRequest, CreateSessionRequest,
    EndSessionRequest, ExclusiveSnapshot, GreetingView, Health, HistoryResponse,
>>>>>>> origin/codex/approval-before-dispatch
''',
              '''    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, GreetingView, Health,
    HistoryResponse,
''')
          replace(
              'crates/ene-api/src/client.rs',
              '''<<<<<<< HEAD
    ResourceKind, RestoreRequest, ScheduleView, SendMessageResponse, SessionPatch, SessionView,
    SetActiveProviderAssetRequest, SetActiveProviderAssetResponse, SoulPatch, SoulSkillsPatch,
    SoulView, SpanView, SplitSessionResponse, StageView, ToolTestRequest, ToolView, UsageView,
=======
    ResourceKind, RestoreRequest, ScheduleView, SelectGreetingRequest, SelectGreetingResponse,
    SendMessageResponse, SessionPatch, SessionView, SetActiveProviderAssetRequest,
    SetActiveProviderAssetResponse, SoulPatch, SoulSkillsPatch, SoulView, SpanView,
    SplitSessionResponse, StageView, ToolTestRequest, ToolView, UsageView,
>>>>>>> origin/codex/approval-before-dispatch
''',
              '''    ResourceKind, RestoreRequest, ScheduleView, SelectGreetingRequest, SelectGreetingResponse,
    SendMessageResponse, SessionPatch, SessionView, SetActiveProviderAssetRequest,
    SetActiveProviderAssetResponse, SoulPatch, SoulSkillsPatch, SoulView, SpanView,
    SplitSessionResponse, StageView, ToolTestRequest, ToolView, UsageView,
''')

          replace(
              'crates/ene-api/src/lib.rs',
              '''<<<<<<< HEAD
    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, Health, HistoryResponse,
    IdempotentMessage, InstallProviderAssetRequest, InstallProviderAssetResponse, JobView,
    ListProviderAssetsRequest, ListProviderAssetsResponse, ListProviderModelsRequest,
    ListProviderModelsResponse, ListenRequest, McpDocument, McpServerView, MemoryPatch, MemoryView,
    MessageMode, MessageRequest, MessageResponse, OccupantView, Page, PluginConfigErrorView,
    PluginConfigField, PluginConfigOptionView, PluginConfigOptionsView, PluginConfigValidateView,
    PluginConfigValues, PluginConfigView, PluginView, Problem, ProviderAssetInstallPhase,
=======
    AffectView, AnswerJobRequest, ApprovalView, ArtifactView, BackupResponse, CharacterView,
    ClaimResourceRequest, CompactResponse, CreateScheduleRequest, CreateSessionRequest,
    EndSessionRequest, ExclusiveSnapshot, GreetingView, Health, HistoryResponse, IdempotentMessage,
    InstallProviderAssetRequest, InstallProviderAssetResponse, JobView, ListProviderAssetsRequest,
    ListProviderAssetsResponse, ListProviderModelsRequest, ListProviderModelsResponse,
    ListenRequest, McpDocument, McpServerView, MemoryPatch, MemoryView, MessageMode,
    MessageRequest, MessageResponse, OccupantView, Page, PluginConfigErrorView, PluginConfigField,
    PluginConfigOptionView, PluginConfigOptionsView, PluginConfigValidateView, PluginConfigValues,
    PluginConfigView, PluginView, Problem, ProviderAssetInstallPhase,
>>>>>>> origin/codex/approval-before-dispatch
''',
              '''    AffectView, AnswerJobRequest, AnswerQuestionRequest, ApprovalView, ArtifactView,
    BackupResponse, CharacterView, ClaimResourceRequest, CompactResponse, CreateScheduleRequest,
    CreateSessionRequest, EndSessionRequest, ExclusiveSnapshot, GreetingView, Health,
    HistoryResponse, IdempotentMessage, InstallProviderAssetRequest, InstallProviderAssetResponse,
    JobView, ListProviderAssetsRequest, ListProviderAssetsResponse, ListProviderModelsRequest,
    ListProviderModelsResponse, ListenRequest, McpDocument, McpServerView, MemoryPatch, MemoryView,
    MessageMode, MessageRequest, MessageResponse, OccupantView, Page, PluginConfigErrorView,
    PluginConfigField, PluginConfigOptionView, PluginConfigOptionsView, PluginConfigValidateView,
    PluginConfigValues, PluginConfigView, PluginView, Problem, ProviderAssetInstallPhase,
''')

          replace(
              'crates/ene-work/src/router.rs',
              '''<<<<<<< HEAD
            let job_id = value
                .get("delegation_id")
                .and_then(Value::as_str)
                .ok_or_else(|| {
                    KernelError::Tool("delegate.start did not return a delegation_id".to_owned())
                })?;
            return Ok(SurfaceToolOutcome::Delegated {
                speech: "I'll look into that.".to_owned(),
                job_id: job_id.to_owned(),
=======
            let speech = if kind == SurfaceCallKind::Upgrade {
                format!("That action requires a Work job, so I started one for `{name}`.")
            } else {
                "I'll look into that.".to_owned()
            };
            return Ok(SurfaceToolOutcome::Delegated {
                speech,
                job_id: job.id.to_string(),
>>>>>>> origin/codex/approval-before-dispatch
''',
              '''            let job_id = value
                .get("delegation_id")
                .and_then(Value::as_str)
                .ok_or_else(|| {
                    KernelError::Tool("delegate.start did not return a delegation_id".to_owned())
                })?;
            let speech = if kind == SurfaceCallKind::Upgrade {
                format!("That action requires a Work job, so I started one for `{name}`.")
            } else {
                "I'll look into that.".to_owned()
            };
            return Ok(SurfaceToolOutcome::Delegated {
                speech,
                job_id: job_id.to_owned(),
''')

          for path in [
              'apps/ene-core/src/http/routes.rs',
              'crates/ene-api/src/client.rs',
              'crates/ene-api/src/lib.rs',
              'crates/ene-work/src/router.rs',
          ]:
              text = Path(path).read_text()
              if '<<<<<<<' in text or '=======' in text or '>>>>>>>' in text:
                  raise SystemExit(f'unresolved conflict marker in {path}')
          PY
          rm -f tmp/merge-1011-conflicts.txt
          cargo fmt --all
          git add -A
          git commit --no-edit
          git push origin HEAD:codex/ask-user-protocol
