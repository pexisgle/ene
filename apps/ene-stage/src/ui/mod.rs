//! Slint Stage surfaces: overlay shell, chat, caption, spotlight, detail.

pub use ene_stage_ui::{
    CaptionSurface, ChatRow, ChatSurface, ChatTargetRow, DetailAction, DetailListRow,
    DetailSurface, EneTheme, OverlayChoice, SpotlightEntry, SpotlightSurface, StageOverlay,
};

pub mod theme;
